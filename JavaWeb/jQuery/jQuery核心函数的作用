### 核心函数的四个作用：

#### 传入参数为[函数]时：在文档加载完成后执行这个函数

```javascript
$(function () {//表示代码执行在页面加载之后

})
```

#### 传入参数为[HTML字符串]时：根据这个字符串创建元素节点对象

没有案例不做演示

### 传入参数为[选择器字符串]时：根据这个字符串查找元素节点对象

```javascript
var $1 = $(btn);
```

#### 传入参数为[DOM对象]时：将DOM对象包装为jQuery对象返回

```javascript
var btn = document.getElementById("01");
var $1 = $(btn);
alert($1);
```